# 실습

현재까지 강의에서 확인한 주요 실습 키워드:

- LangChain `create_agent()`
- LangGraph `StateGraph`
- `add_node()`, `add_edge()`
- `Command(update=..., goto=...)`
- `@tool` + docstring
- ChromaDB Retriever
- `search_kwargs={"k": 1}`
- Structured Output / `ToolStrategy`
- `LlamaCpp` + GGUF
- GPU offloading (`n_gpu_layers=-1`)
